# Tracked scripts (portable)

These are committed so a laptop `git pull` gets apply helpers. `_tools/` stays gitignored (UnrealPak, extracts, local rebuild).

| File | Purpose |
|------|---------|
| `recipe_grok_advanced_deep_ore_scanner.json` | Advanced Deep Ore Scanner recipe (Machining/Fab/Manufacturer) |
| `ensure_adv_deep_ore_scanner.py` | Idempotent insert/replace into extracted `D_ProcessorRecipes.json` |

See `docs/ADVANCED_DEEP_ORE_SCANNER.md` and `docs/LAPTOP_SYNC.md`.
